@page
@using SampleDataLibrary.Data
@model NorthwindM.Pages.NombresModel
@{
}

<div class="row">
    <h1 class="display-2">Categorias</h1>
    <table class="table">
        <thead class="thead-inverse">
            <tr>
                <th>Nombre de la categoria</th>
                <th>Descripcion</th>
               
            </tr>
        </thead>
        <tbody>
            @if (Model.categorias is not null)
            {
                @foreach(Category categoria in Model.categorias)
                {
                    <tr>
                        <td>@categoria.CategoryName</td>                    
                        <td>@categoria.Description</td>                    
                        
                    </tr>
                }
            }
        </tbody>
    </table>
</div>